/**
 * Better Auth Server logic, Drizzle ORM, and Neon PostgreSQL database handlers
 * are implemented in the Express Backend (deploy/src/lib/auth.ts).
 * 
 * For Frontend Client Auth, import authClient from '@/lib/auth-client'.
 */
export { authClient } from './auth-client';
